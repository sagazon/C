#include <stdio.h>

int main() {
    // This program adds two predefined integers and prints the result

    int x = 10, y = 22, z;
    z = x + y;

    printf("The value of z is %d", z);

    return 0;
}
