#include <stdio.h>

int main() {
    // This program performs a mathematical expression with multiplication and addition

    int x = 8, y = 2, z = 0, q;
    q = x * y + z;

    printf("The value of q is %d", q);

    return 0;
}
