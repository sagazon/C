#include <stdio.h>

// Function to calculate and print the square of a
void square() {
    int a = 5;
    int b = a * a;  // Calculating the square
    printf("The square of a is = %d", b);  // Printing the result
}

int main() {
    square();  // Calling the square function
    return 0;
}
