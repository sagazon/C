#include <stdio.h>

int main() {
    // This program calculates the area of a circle using a fixed radius

    float pi = 3.14; 
    int r = 2;
    float c = pi * r * r;

    printf("The value of c is %f", c);

    return 0;
}
